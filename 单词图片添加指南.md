# 单词图片添加指南

## 目标
为每个单词添加示意图片，帮助小学生更好地理解和记忆单词。

## 方案设计

### 方案说明
- 图片存储在 `public/images/words/` 目录
- 图片命名规则：使用单词名称（小写）作为文件名，例如：`apple.png`、`cat.jpg`
- Word 类型中添加可选的 `image` 字段支持自定义图片路径
- 组件会自动查找并显示图片，如果不存在则隐藏图片区域

## 操作步骤

### 步骤 1：创建图片存储目录

在项目根目录的 `public` 文件夹下创建图片目录：

```bash
mkdir -p public/images/words
```

### 步骤 2：准备单词图片

1. **图片格式**：推荐使用 PNG 或 JPG 格式
2. **图片大小**：建议 200x200px 到 400x400px
3. **图片命名**：
   - 使用单词的英文名称（小写）
   - 例如：`apple.png`、`cat.jpg`、`dog.png`
   - 如果单词包含空格或特殊字符，使用下划线替换，例如：`ice_cream.png`

4. **图片内容**：可以是：
   - 实物照片（如 apple、cat）
   - 简笔画、插画
   - 图标
   - 示意图

5. **将图片放入目录**：
   ```bash
   # 示例
   cp your-images/apple.png public/images/words/
   cp your-images/cat.jpg public/images/words/
   cp your-images/dog.png public/images/words/
   ```

### 步骤 3：修改代码（已自动完成）

以下代码修改已经完成，你只需要了解即可：

#### 3.1 修改 Word 类型定义（可选）
如果需要支持自定义图片路径，可以在 `src/types.ts` 中添加：
```typescript
export type Word = {
  "name": string,
  "usphone": string,
  "ukphone": string,
  "trans": string[]
  checked?: boolean,
  id?: any,
  image?: string, // 可选的自定义图片路径
}
```

#### 3.2 修改显示组件
在 `src/pages/practice/practice-word/Typing.vue` 中添加图片显示逻辑。

### 步骤 4：测试

1. 启动项目：
   ```bash
   npm start
   ```

2. 选择一个有图片的单词进行测试
3. 检查图片是否正确显示

## 批量添加图片的建议

### 方法 1：手动添加
- 逐个单词查找或制作图片
- 按照命名规则保存到 `public/images/words/` 目录

### 方法 2：使用脚本批量处理
可以编写脚本自动下载或处理图片（需要额外的工具支持）

### 方法 3：分阶段添加
- 优先添加常用单词的图片
- 逐步完善其他单词的图片

## 图片获取建议

1. **免费图片资源**：
   - Unsplash (https://unsplash.com)
   - Pexels (https://www.pexels.com)
   - Pixabay (https://pixabay.com)
   - Flaticon (https://www.flaticon.com) - 图标类

2. **AI 生成图片**：
   - 使用 DALL-E、Midjourney 等 AI 工具生成定制图片

3. **自己绘制**：
   - 使用绘图软件（如 Figma、Sketch）绘制简笔画

## 注意事项

1. **版权问题**：确保使用的图片有合法使用权
2. **图片大小**：建议压缩图片以优化加载速度
3. **命名一致性**：严格按照单词名称（小写）命名，保持一致性
4. **文件扩展名**：统一使用 `.png` 或 `.jpg`，建议统一使用 `.png`

## 示例

假设你要为单词 "apple" 添加图片：

1. 准备图片文件：`apple.png`
2. 保存到：`public/images/words/apple.png`
3. 在代码中会自动识别并显示

单词名称会自动匹配文件名，例如：
- 单词 "Apple" → 查找 `apple.png`
- 单词 "CAT" → 查找 `cat.png`（自动转小写）
